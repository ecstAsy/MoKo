<block wx:for="{{Comment}}" wx:for-item="star" wx:key="index">
  <Star title="{{star.title}}" StarId="{{star.flag}}" data-id="{{index}}" bind:select="ActionStar"/>
</block>

<card-slide title="{{title}}" content="{{content}}" />